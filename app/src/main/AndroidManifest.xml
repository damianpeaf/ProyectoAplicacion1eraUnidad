<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectofinal1eraunidad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".creditos"></activity>
        <activity android:name=".area" />
        <activity android:name=".elipse" />
        <activity android:name=".logaritmica" />
        <activity android:name=".circunferencia" />
        <activity android:name=".exponencial" />
        <activity android:name=".cubica" />
        <activity android:name=".hiperbola" />
        <activity android:name=".cuadratica" />
        <activity android:name=".parabola" />
        <activity android:name=".Lineales" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>